package main

import "fmt"

func main() {
	target := 36000000
}

func findHouse(target int) int {
	seen := map[int]int{
		1: 10,
	}
	numOfPresents := 0
	for house := 2; numOfPresents < target; house++ {
		if house % 2 == 0 {
			
		}
	}
}